# ===========================================
# FeedDigest — Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha os valores
# cp .env.example .env

# --- PostgreSQL ---
POSTGRES_USER=feeddigest
POSTGRES_PASSWORD=feeddigest123
POSTGRES_DB=feeddigest
DATABASE_URL=postgresql://feeddigest:feeddigest123@postgres:5432/feeddigest

# --- Redis ---
REDIS_URL=redis://redis:6379

# --- Groq (LLM - compatível com formato OpenAI) ---
GROQ_API_KEY=gsk_your_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# --- JWT (usado pelo Gateway) ---
JWT_SECRET=troque_isso_por_uma_string_segura

# --- Portas dos serviços ---
GATEWAY_PORT=3000
FEED_SERVICE_PORT=8001
SUMMARIZER_PORT=8002

# --- Scheduler ---
FETCH_INTERVAL_MINUTES=30
